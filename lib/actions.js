const adder = (x, y) => x + y
const subtracter = (x, y) => x - y
const multiplier = (x, y) => x * y
const divider = (x, y) => x / y

module.exports = {
  adder,
  subtracter,
  multiplier,
  divider,
}
